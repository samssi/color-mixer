import React from "react";
import ReactDom from "react-dom";
import ColorMixerApp from "./ColorMixerApp";

const rootElement = document.getElementById("root");

ReactDom.render(
    <ColorMixerApp />,
    rootElement
)
